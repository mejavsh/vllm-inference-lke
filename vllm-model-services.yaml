apiVersion: v1
kind: Service
metadata:
  name: vllm-8b-service
spec:
  type: ClusterIP
  selector:
    app: vllm
    model: 8b
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: vllm-70b-service
spec:
  type: ClusterIP
  selector:
    app: vllm
    model: 70b
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
